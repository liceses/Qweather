# 代码走读要点清单

> 录制时翻代码用的高亮点清单。每个条目标注了文件路径+行号，方便录屏时快速定位。

---

## 1. 入口与依赖注入

### 文件: `main.cpp` (18-76)

| 行号 | 要点 |
|------|------|
| 20 | `qputenv("QT_QUICK_CONTROLS_STYLE", "Fusion")` — 设置 Fusion 主题 |
| 25-26 | `qRegisterMetaType<QJsonArray>()` — 注册元类型，QML 才能传递 QJsonArray |
| 28-54 | **10 个对象的依赖链** — 截全屏展示从 WeatherCache → WeatherAPI → 4 个 Store → GlobalClock → BackgroundManager → TransitionController 的依赖关系 |
| 56 | `QQmlApplicationEngine engine` — QML 引擎创建 |
| 63-72 | **10 次 setContextProperty** — 将 C++ 对象注入 QML 全局命名空间 |
| 73 | `engine.loadFromModule("qml1", "Main")` — 加载 Main.qml |

---

## 2. 统一渲染状态契约

### 文件: `SkyState.h` (1-87)

| 行号 | 要点 |
|------|------|
| 7 | `struct SkyState { Q_GADGET` — 值类型结构体，非 QObject，轻量 |
| 9-32 | 20 个 `Q_PROPERTY` 按 4 组排列：天文/大气/天气/变体 |
| 36-61 | 20 个成员变量的默认值 |
| 64-84 | `dump()` — 调试输出所有字段 |

---

## 3. API 封装层

### 文件: `weatherApi.h` (37-171)

| 行号 | 要点 |
|------|------|
| 10-35 | `enum class ApiRequestType` — 24 个请求类型枚举 |
| 46-87 | 24 个 `Q_INVOKABLE` 方法 — 所有可被 QML 调用的 API 函数 |

### 文件: `weatherApi.cpp` (11-607)

| 行号 | 要点 |
|------|------|
| 11-39 | `cacheConf()` — 每个 API 的缓存 TTL 配置表 |
| 41-44 | 构造函数 — 创建 QNetworkAccessManager |
| 387-444 | `onReplyFinished()` — **统一路由核心**：缓存写入 + Handler 分发 |
| 445-451 | `sendRequest()` — 在请求属性中标记类型 |
| 455-607 | 18 个 `handle*` 函数 — 提取 JSON 并发射信号 |

---

## 4. SQLite 缓存层

### 文件: `WeatherCache.h` (10-35)

| 行号 | 要点 |
|------|------|
| 10-35 | 6 个接口方法 |

### 文件: `WeatherCache.cpp` (19-37)

| 行号 | 要点 |
|------|------|
| 21-23 | 数据库文件路径 `{exeDir}/weather_cache.db` |
| 32-36 | CREATE TABLE 语句 — k(主键)/data(JSON)/ts(时间戳) |
| 39-57 | `get()` — **TTL 过期检测核心**：比较 now-ts > ttlSeconds 则删除并返回空 |

---

## 5. 数据 Store 层

### 5.1 ForecastStore — 预报数据

### 文件: `ForecastStore.cpp` (62-124)

| 行号 | 要点 |
|------|------|
| 62-93 | `onWeatherDailyReady()` — 解析 fxDate→x, tempMax→yMax, tempMin→yMin |
| 82-92 | **写时复制模式**：`QVariantMap newData = m_chartData; ... m_chartData = newData;` |
| 95-124 | `onWeatherHourlyReady()` — fxTime→x, temp→y |

### 5.2 CityDetailStore — 10 路 API 聚合

### 文件: `CityDetailStore.cpp` (7-369)

| 行号 | 要点 |
|------|------|
| 7-19 | 构造函数预初始化 10 个数据段为空对象 |
| 49-79 | `setCity()` → fetchAll() |
| 81-99 | `fetchAll()` — **并行发起 10 个 API 请求** |
| 102-104 | `emitUpdated()` — 每次创建新 QVariantMap 确保 QML 绑定检测到引用变化 |
| 108-128 | `onWeatherNowReady()` — 每个 handler 先检查 `_location == m_cityId` 过滤 |

---

## 6. 天空模拟系统核心

### 6.1 GlobalClock — 60fps 时间源

### 文件: `GlobalClock.cpp` (1-52)

| 行号 | 要点 |
|------|------|
| 8-9 | `QTimer` 16ms 驱动 + `QElapsedTimer` 精确计时 |
| 28 | `m_pauseOffset = m_elapsed` — 暂停时冻结时间 |
| 47 | `secs = m_pauseOffset + m_elapsedTimer.elapsed() / 1000` — 暂停恢复计算 |

### 6.2 AstronomyModel — 天文计算

### 文件: `AstronomyModel.cpp` (106-130)

| 行号 | 要点 |
|------|------|
| 110 | `declination = 23.45 * sin(2π * (dayOfYear - 81) / 365)` — 太阳赤纬公式 |
| 113 | `hourAngle = (minuteOfDay - 720) * 0.25` — 时角计算 |
| 120-122 | `sinAlt = sin(lat)*sin(decl) + cos(lat)*cos(decl)*cos(ha)` — 高度角 |
| 125-129 | 方位角计算 + 下午偏西修正 |
| 69-73 | 月亮位置简化：太阳对跖点 + 15° 偏移 |

### 6.3 BackgroundManager — 中央控制器

### 文件: `BackgroundManager.cpp` (31-397)

| 行号 | 要点 |
|------|------|
| 31-83 | `commitSkyState()` — 3 个 lambda 解析器更新 20 个字段 |
| 87-117 | `updateWeather()` — 天气码→渲染参数的完整映射 |
| 91-106 | **曝光计算**：天气缩放因子 `weatherScale = cloudDim × rainDim × snowDim × fogDim` |
| 266-276 | `buildAstronomyChanges()` — 6 个天文字段 |
| 278-353 | **`buildAtmosphereChanges()` — 核心大气颜色模型**：7 段插值 + 曝光/星星/黄昏因子分段计算 |
| 283-292 | 7 段颜色表格 (deep night / blue hour / gold hour / day / warm noon / orange / purple) |
| 243-262 | `tickLerp()` — 60 帧 smoothstep 位置插值过渡 |
| 373-397 | `saveProfileForCode()` — 将当前调校保存为天气码配置 |

### 6.4 TransitionController — 图层编排

### 文件: `TransitionController.cpp` (92-307)

| 行号 | 要点 |
|------|------|
| 92-181 | `setSkyState()` — 4 个图层的激活/去激活判断 |
| 98-99 | 迟滞阈值：激活 0.015 > 去激活 0.005 |
| 102-123 | Cloud 层逻辑：覆盖率达阈值则激活，达 0 则去激活 |
| 126-150 | Weather 层逻辑：延迟 400ms（如果云已激活）|
| 185-259 | `activateLayer()` — 延迟 QTimer → 先 active=true → 再 tp=1.0 |
| 263-292 | `deactivateLayer()` — tp=0 触发 Behavior → 延迟销毁 Loader |

---

## 7. QML 前端

### 7.1 Main.qml — 主窗口

### 文件: `Main.qml` (1-467)

| 行号 | 要点 |
|------|------|
| 13-19 | 6 个核心数据属性 |
| 86-103 | `promoteCity()` — 栈式城市列表管理，长度限制 maxCards+1 |
| 128-143 | `onFocusIdChanged` — 焦点变更触发 5 个 API 调用 |
| 146-167 | 3 层叠加：WeatherBackground(z:-1) + 暗色遮罩(z:0) + DebugPanel(z:100) |
| 152-162 | 暗色遮罩 opacity 公式 `max(0, min((exp-1.2)/0.6, 1)) × 0.2` |
| 170-183 | 视差效果：鼠标位置 → -1~1 归一化 |
| 189-276 | 侧边栏：可折叠 NavButton + 固定城市列表 |
| 279-358 | StackLayout 7 个页面 + 交叉淡入淡出 |
| 386-428 | Connections 信号中枢 |

### 7.2 WeatherBackground.qml — 图层栈

### 文件: `WeatherBackground.qml` (1-144)

| 行号 | 要点 |
|------|------|
| 16-33 | SkyLayer — 始终渲染，15 个属性绑定 |
| 35-43 | AtmosphereLayer — 始终渲染 |
| 46-77 | 4 个 `Loader` — `active` 绑定 `transitionCtrl.xxxActive`，`Behavior on opacity` 动画 |
| 80-138 | 4 个 `Component` — 条件加载的图层定义 |

### 7.3 WeatherBackgroundDebugPanel.qml

### 文件: `WeatherBackgroundDebugPanel.qml` (1-659)

| 行号 | 要点 |
|------|------|
| 21-44 | `commit()` — 收集所有控件值提交完整 SkyState |
| 58-87 | `switchWeather()` — 天气码切换 + 参数插值过渡 |
| 89-114 | 自动演示 timer — 时间前进 + 天气码循环 |
| 136-148 | Ctrl+Shift+B 快捷键开关 |

---

## 8. 着色器

### 文件: `shaders/sky.frag` (1-148)

| 行号 | 要点 |
|------|------|
| 4-22 | UB0 布局 — 20 个 uniform |
| 28-35 | ACES 色调映射（内联） |
| 59-73 | `starField()` — 50 个粒子 + 正弦闪烁 |
| 75-89 | `solarScreenPos()` / `moonScreenPos()` — 屏幕空间投影 + 视差偏移 |
| 91-99 | `moonPhaseMask()` — 月相 SDF |
| 101-148 | `main()` — 渲染管线 |

### 文件: `shaders/cloud.frag` (1-145)

| 行号 | 要点 |
|------|------|
| 52-62 | `fbm2D()` — 多 octave fBM |
| 80-82 | 变体密度缩放 |
| 85-89 | 域扭曲 |
| 95-103 | 3 层 fBM |
| 107-119 | Emptiness/Sharpness 重映射 |
| 122-126 | 4 步光线步进 |

---

## 9. QML 可复用组件

| 文件 | 高亮点 | 行号 |
|------|--------|------|
| `NavButton.qml` | 折叠/展开动画 + MultiEffect 图标着色 | 16-26, 52-57 |
| `SearchBar.qml` | 300ms 防抖 + 键盘导航 + 结果过滤 | 114-135, 315-325, 327-335 |
| `WeatherIcon.qml` | 天气码→SVG URL 映射 | 29-58 |
| `CityCard.qml` | 双击检测：400ms Timer | 80-107 |
| `ForecastCard.qml` | 预声明 3 个 Series + updateChart() | 89-103, 126-170 |

---

## 10. 构建配置

### 文件: `CMakeLists.txt` (1-104)

| 行号 | 要点 |
|------|------|
| 7 | 6 个 Qt 模块依赖 |
| 19-62 | `qt_add_qml_module` — 28 个 QML + 14 对 C++ 文件 |
| 64-74 | `qt_add_resources` — 7 个 SVG 图标 |
| 77-87 | `qt_add_shaders` — 7 个 .frag → .qsb |
| 96-98 | 5 个链接库 |
